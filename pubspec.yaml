name: shows_vault
description: "Discover your favorite movies and TV shows"
publish_to: "none"
version: 0.1.1

environment:
  sdk: 3.5.0
  flutter: 3.24.0

dependencies:
  flutter:
    sdk: flutter
  freezed_annotation: ^2.4.4
  json_annotation: ^4.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  husky: ^0.1.7
  commitlint_cli: ^0.7.2
  danger_core: ^2.0.0
  build_runner: ^2.4.13
  freezed: ^2.5.7
  json_serializable: ^6.8.0

flutter:
  uses-material-design: true
